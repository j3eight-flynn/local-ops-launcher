# __APP_NAME__ Operator Guide

This generated launcher is review-first. Edit `launcher-projects.json`, then run:

```sh
./scripts/test-launcher-registry.sh
./scripts/release-check.sh
```

Do not add stop or repair commands until you know they are safe for the project.
